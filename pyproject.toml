[project]
name = "roblox-revenue-intelligence"
version = "0.1.0"
description = "Evidence-driven Roblox monetization opportunity scoring"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
